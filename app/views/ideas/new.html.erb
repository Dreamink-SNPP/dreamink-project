<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- Header -->
  <div class="mb-8">
    <div class="flex items-center space-x-3 mb-2">
      <%= link_to project_ideas_path(@project), class: "text-gray-400 hover:text-gray-500" do %>
        <%= heroicon "chevron-left", size: "5" %>
      <% end %>
      <h1 class="text-3xl font-bold text-gray-900 flex items-center gap-2">
        <%= heroicon "light-bulb", size: "8", class: "text-yellow-500" %> Nueva Idea
      </h1>
    </div>
    <p class="text-sm text-gray-600">
      Proyecto: <span class="font-medium"><%= @project.title %></span>
    </p>
    <p class="mt-2 text-sm text-gray-500">
      Captura ideas sueltas, diálogos, giros argumentales, o cualquier cosa que
      se te ocurra mientras desarrollas tu historia.
    </p>
  </div>

  <!-- Formulario -->
  <%= render "form", idea: @idea %>
</div>
