<!-- Estadísticas rápidas -->
<div class="mt-4 flex space-x-6 text-sm text-gray-600">
  <div class="flex items-center">
    <span class="mr-1 font-semibold"><%= acts_count %></span>
    <%= acts_count == 1 ? "Acto" : "Actos" %>
  </div>
  <div class="flex items-center">
    <span class="mr-1 font-semibold"><%= sequences_count %></span>
    <%= sequences_count == 1 ? "Secuencia" : "Secuencias" %>
  </div>
  <div class="flex items-center">
    <span class="mr-1 font-semibold"><%= scenes_count %></span>
    <%= scenes_count == 1 ? "Escena" : "Escenas" %>
  </div>
</div>
